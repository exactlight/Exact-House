# Deploys to a *.netlify.app preview URL. No custom domain wired up.
# Base directory on the Netlify site should be set to sites/lingolaunch.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
