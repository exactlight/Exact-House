# Deploys to a *.netlify.app preview URL only.
# exacthouse.com DNS is NOT pointed here until launch is approved and tested.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
